{
  "name": "ai-agents",
  "version": "0.1.0",
  "description": "REST API wrapper that executes Perplexity-backed vigilance prompts.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
